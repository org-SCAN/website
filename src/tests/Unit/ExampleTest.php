<?php

namespace Tests\Unit;

use PHPUnit\Framework\TestCase;

class ExampleTest extends TestCase
{
    /**
     * WARNING : this file has to be present else Unit directory will be ignored by .gitignore causing the automated test (on github) to fail
     * It can be removed once there is at least one test in the Unit directory
     */
    /**
     * A basic unit test example.
     *
     * @return void
     */
    public function test_example()
    {
        $this->assertTrue(true);
    }
}
